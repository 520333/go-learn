syntax = "proto3";
package proto;

option go_package = ".;helloworld"; // 新版必须要有

service Hello{
  rpc Hello(HelloRequest) returns(Response); // Hello接口
}

message HelloRequest {
  string name = 1;
  int32 age = 2;
  repeated string courses = 3;
}
message Response{
  string reply =1;

}
