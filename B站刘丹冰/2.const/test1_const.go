package main

/*
import "fmt"

// iota 只能配合const常量使用 无法用于变量
const length int = 10 // 常量只读属性

//const 定义枚举
const ( // 可以在const()添加一个关键iota 每行的iota都会累加1 第一行的iota的默认值是0
	BEIJING   = 10 * iota //iota = 0
	SHANGAHI              //iota = 10
	GUANGZHOU             //iota = 20
	FUZHOU                //iota = 30
)

const (
	a, b = iota + 1, iota + 2 // iota = 0, a = 1, b = 2
	c, d                      // iota = 1, c = iota + 1, d = iota + 2
	e, f                      // iota = 2, e = iota + 1, f = iota + 2
	g, h = iota * 2, iota * 3 // iota = 3, g = iota * 2, h = iota * 3
	i, k                      // iota = 4, i = iota * 2, k = iota * 3
)

myfunc main() {

	fmt.Println(length)
	// length2 = 233 // 常量不允许修改
	fmt.Println(BEIJING, SHANGAHI, GUANGZHOU, FUZHOU)
	fmt.Println("a = ", a)
	fmt.Println("b = ", b)
	fmt.Println("c = ", c)
	fmt.Println("d = ", d)
	fmt.Println("e = ", e)
	fmt.Println("f = ", f)
	fmt.Println("g = ", g)
	fmt.Println("h = ", h)
	fmt.Println("i = ", i)
	fmt.Println("k = ", k)
}
*/
