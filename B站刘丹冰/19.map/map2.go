package main

/*
import "fmt"

myfunc printMap(cityMap map[string]string) {
	//printMap中的cityMap是一个引用传递
	for key, value := range cityMap {
		fmt.Printf("key=%s value = %s\n", key, value)
	}
}

myfunc ChangeValue(cityMap map[string]string) {
	cityMap["England"] = "Lodon"
}
myfunc main() {
	cityMap := make(map[string]string)
	cityMap["China"] = "Beijing" //添加
	cityMap["Japan"] = "Tokyo"
	cityMap["USA"] = "NewYork"

	//遍历
	printMap(cityMap)

	delete(cityMap, "China") //删除
	fmt.Println("==============")

	cityMap["USA"] = "DC" //修改
	ChangeValue(cityMap)
	for key, value := range cityMap { //遍历
		fmt.Printf("key=%s value = %s\n", key, value)
	}

}*/
