package main

/*
import "fmt"

myfunc main() {
	//slice1 := []int{1, 2, 3} //声明slice是一个切片并且初始化，默认值是1，2，3 长度len是3
	var slice1 []int //声明slice1是一个切片，但是并没有给slice分配空间
	//slice1 = make([]int, 3) // 开辟3个空间，默认值是0
	//slice1[0] = 100
	//var slice1 []int = make([]int, 3) //声明slice是一个切片并且初始化 分配3个空间 初始化值是0
	//slice1 := make([]int, 3)
	fmt.Printf("len = %d, slice = %v\n", len(slice1), slice1)
	if slice1 == nil {
		fmt.Println("slice1是一个空切片")
	} else {
		fmt.Println("slice1是有空间的")
	}
}*/
