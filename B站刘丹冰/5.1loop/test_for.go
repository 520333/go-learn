package main

/*
import (
	"fmt"
	"time"
)

myfunc main() {
	// for init; condition; post{}
	// var i int
	// for i < 3 {
	// 	time.Sleep(1 * time.Second)
	// 	a := time.Now().Format("2006-01-02 15:04:05")
	// 	fmt.Println("ok", a)
	// 	i++
	// }

	// 1-100相加的结果
	var sum int
	for i := 1; i <= 100; i++ {
		sum += i
	}
	fmt.Println(sum)

	// 九九乘法表
	for y := 1; y <= 9; y++ {
		for x := 1; x <= y; x++ {
			fmt.Printf("%d*%d=%d\t", x, y, x*y)
		}
		fmt.Println()
	}

	// for range 主要对字符串、数组、切片、map、channel做处理
	name := "kubernetes入门"
	nameRune := []rune(name)
	for index, value := range name {
		fmt.Printf("%d --- %c\r\n", index, value)
	}
	for i := 0; i < len(nameRune); i++ {
		fmt.Printf("%c\r\n", nameRune[i])
	}

	for _, index := range name {
		fmt.Printf(" --- %c\r\n", index)
	}

	round := 0
	for {
		time.Sleep(1 * time.Second)
		round++
		if round == 5 {
			continue
		}
		fmt.Println(round)
		if round > 10 {
			break
		}
	}
}
*/
